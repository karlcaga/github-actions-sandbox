name: "Test and Lint"

on: [push, pull_request]

jobs:
  test:
    name: Simulate a test 
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: >-
          echo Testing...
          exit 1
          echo Should not reach this message
